# Scripts de ingesta (ETL)

Cada fuente de datos vive en su propio archivo/carpeta.
Escribí solo vía el contrato acordado (upsert person/company + relationships).
No inventes DNI/CUIT. Guardá siempre `source_url`.
